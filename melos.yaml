name: flutter_advanced_course_multi_modular_clean_architecture
packages:
  # Include all modules in the workspace
  #  - 'core'
  #  - 'feature1'
  #  - 'feature2'
  # You can also use wildcards for flexibility, like:
  - core/**
  # - features/movies
  - features/**

command:
  bootstrap:
    environment:
      sdk: ^3.5.3
      flutter: ">=1.17.0"
    dependencies:
      dio: 5.8.0+1

    dev_dependencies:
      build_runner: ^2.4.15


# Define dependency version constraints that will apply across the workspace
#dependencies:
##  shared_dependencies:
##    dio: ^5.0.0
## Centralized dev dependencies
#dev_dependencies:
#  flutter_test:
#    sdk: flutter
#
#dependency_overrides:
#  dio: ^5.0.0

  # Optional: Define scripts to run tasks across the workspace
scripts:
  bootstrap: melos bootstrap
  clean: melos clean
  update: dart pub outdated